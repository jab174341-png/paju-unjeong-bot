# 파주 운정 시세 대시보드 - 사용 가이드

> 미래의 나를 위한 안내서. 시간 지나서 잊어버려도 이거 보면서 재개할 수 있게.

---

## 🌐 주요 주소

| 종류 | URL |
|---|---|
| **사이트 (라이브)** | https://paju-unjeong-bot.onrender.com |
| **코드 저장소 (GitHub)** | https://github.com/jab174341-png/paju-unjeong-bot |
| **호스팅 대시보드 (Render)** | https://dashboard.render.com |
| **API 키 발급처** | https://www.data.go.kr |

## 📂 로컬 폴더 위치

```
/Users/sangbeom/Desktop/에이전트/클로드/코드 작업/paju-unjeong-bot/
```

---

## 🔄 다시 작업하려면 (터미널)

매번 작업 전에 이 3줄을 실행하세요:

```bash
cd "/Users/sangbeom/Desktop/에이전트/클로드/코드 작업/paju-unjeong-bot"
source venv/bin/activate
ls
```

→ 가상환경 켜지고 파일 목록이 보이면 OK.

### 로컬에서 미리 테스트하기

```bash
uvicorn app.main:app --reload
```

→ 브라우저에서 http://127.0.0.1:8000 접속.
→ 끄려면 터미널에서 **Ctrl + C**.

---

## ✏️ 자주 하는 수정

### 1. 새 단지 추가하기

파일: `app/data/complexes.py`

맨 아래 ❛사용자 관심 단지❜ 섹션에 한 줄 추가:

```python
{"name": "정확한_단지명", "dong": "법정동명"},
```

⚠️ **단지명은 국토부 API 등록명과 정확히 일치해야 함** (오타 1글자라도 다르면 데이터 매칭 실패).

#### 정확한 단지명 찾는 법

```bash
python -c "
from app.analysis.loader import load_unjeong_trades
df = load_unjeong_trades(months=12)
hits = df[df['apt_name'].str.contains('찾을_단지_키워드')]
print(hits['apt_name'].value_counts())
"
```

예) "OO마을 5단지" 찾고 싶으면 키워드를 `OO마을5단지` 로 검색.

### 2. 색상이나 디자인 바꾸기

파일: `static/style.css`

주요 색상 변수:
- `#3b82f6` (파랑, 강조색) → 카드 hover, 선택된 탭, 차트 라인
- `#dc2626` (빨강) → 가격 상승 표시
- `#2563eb` (파랑) → 가격 하락 표시
- `#1e293b` (검정) → 일반 텍스트

### 3. 표시되는 항목 늘리기/줄이기

파일: `templates/detail.html` (상세 페이지) 또는 `templates/index.html` (홈)

`<div class="stat">` 블록을 복사하거나 지우면 됨.

### 4. 차트 색상 바꾸기

파일: `app/chart/builder.py` 의 `color="#3b82f6"` 부분

---

## 📤 수정 사항 사이트에 반영하기

수정 후 다음 4줄 실행:

```bash
git add .
git commit -m "어떤 수정인지 한 줄"
git push
```

→ Render가 1~2분 후 자동으로 새 버전 배포.
→ Render 대시보드에서 진행 상황 확인 가능.

---

## 🆘 문제 해결

### 사이트가 안 떠요

1. **30초~1분 기다려보기** (무료 플랜은 비활성 시 잠듦, 첫 요청에 깨어남)
2. Render 대시보드 → Logs 확인 → 에러 메시지 있으면 그걸로 검색

### "API 호출 한도 초과" 같은 에러

- 공공데이터포털 개발계정은 일일 1,000건 제한
- 캐시를 더 길게 가져가면 호출 줄일 수 있음 (`app/analysis/loader.py` 의 `max_age_hours`)

### 로컬에서 갑자기 안 돌아가요

```bash
# 가상환경 다시 켜기
source venv/bin/activate

# 라이브러리 다시 설치
pip install -r requirements.txt
```

---

## 🔑 API 키 관리

- **로컬**: `.env` 파일 (이 파일은 GitHub에 안 올라감, 안전)
- **Render 서버**: 대시보드 → Environment → `MOLIT_API_KEY` 환경변수

키가 노출됐다고 의심되면:
1. https://www.data.go.kr 에서 키 재발급
2. 로컬 `.env` 파일에 새 키 입력
3. Render 대시보드 → Environment → 환경변수 값 변경
4. Render 자동 재배포

---

## 💡 아이디어 메모 (앞으로 추가하고 싶은 것들)

- [ ] 뉴스레터: 매일 아침 운정 시세 요약 이메일
- [ ] 단지 비교 페이지 (여러 단지를 한 화면에)
- [ ] 평형 필터 (84㎡대만 보기 등)
- [ ] 전·월세 데이터 추가
- [ ] 호가 vs 실거래가 격차 표시
